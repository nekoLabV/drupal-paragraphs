<div{{ attributes.addClass(classes) }} style="width: 100%;">
  {% if timeline_id is defined %}
  <div 
    data-id="{{ timeline_id }}"
    data-vue-component="timeline">
    {{ content }}
  </div>
  {% else %}
    {# Fallback: 如果没有数据，显示原始内容 #}
    {{ content }}
  {% endif %}
</div>

